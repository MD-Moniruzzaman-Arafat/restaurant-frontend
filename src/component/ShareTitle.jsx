

const ShareTitle = (smallTitle, largeTitle) => {
      return (
            <div>
                  <p>---{smallTitle}---</p>
                  <h1>{largeTitle}</h1>
            </div>
      );
};

export default ShareTitle;